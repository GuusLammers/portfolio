import { Box } from '@mui/material';

export const Random = () => {
  return (
    <Box
      sx={{
        width: '100px',
        height: '100px',
        backgroundColor: 'white',
      }}
    >
      Random
    </Box>
  );
};
